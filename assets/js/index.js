$(document).ready(function(){
  $('.owl-carousel').owlCarousel({
    stagePadding: 100,
    items:1,
    loop:true,
    margin:10,
    autoplay:true,
    autoplayTimeout:5000,
    autoplayHoverPause:true,
    lazyLoad: true,
    // nav:true
  });
});
