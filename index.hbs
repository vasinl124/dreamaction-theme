{{!< default}}
{{! The tag above means - insert everything in this file into the {body} of the default.hbs template }}

{{! The big featured header }}
<header class="main-header {{#if @blog.cover}}" style="background-image: url({{@blog.cover}}){{else}}no-cover{{/if}}">
    <nav class="main-nav overlay clearfix">
        {{#if @blog.logo}}<a class="blog-logo" href="{{@blog.url}}"><img src="{{@blog.logo}}" alt="{{@blog.title}}" /></a>{{/if}}
        <a class="blog-logo" href="{{@blog.url}}"><h4 class='main-logo'>{{@blog.title}}</h4></a>
        
        {{#if @blog.navigation}}
            <a class="menu-button icon-menu" href="#"><span class="word">Menu</span></a>
        {{/if}}
    </nav>
    <div class="vertical">
        <div class="main-header-content inner">
            
            <h1 class="page-title">{{@blog.title}}</h1>
            <h2 class="page-description">{{@blog.description}}</h2>
        </div>
    </div>
    <a class="scroll-down icon-arrow-left" href="#content" data-offset="-10"><span class="hidden">Scroll Down</span></a>
</header>
{{! The main content area on the homepage }}

<main id="content" class="content" role="main">
    <div class='main-tag-container'>        
        {{#get "tags" filter='name:ACTION,name:PERSPECTIVES'}}
            {{#foreach tags}}
                <div style="background-image: url({{image}});" class='main-tag'>
                    <a href='{{url}}'  title="{{name}}" class="tag-{{id}} ">
                        <h4>{{name}}</h4>   
                        <p>{{description}}</p>    
                    </a>    
                </div>
            {{/foreach}}
        {{/get}}
    </div>

    <div class='main-recent-container recent'>
        <h4>ล่าสุด</h4>
    </div>
    {{! The tag below includes the post loop - partials/loop.hbs }}
    {{> "loop"}}    
    
    

</main>
<div class='text-center tag-footer-container'>
            {{#get "tags"}}
            
            {{#foreach tags}}
                <a href='{{url}}'  title="{{name}}" class="tag-{{id}}  tag-footer">
                    | {{name}} |
                </a>    
            {{/foreach}}
        {{/get}}
        </div>
